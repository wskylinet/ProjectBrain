# ---------- .NET / Visual Studio ----------
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/
*.userprefs

# ---------- Node / 前端 ----------
node_modules/
dist/
dist-ssr/
*.local
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- 环境与机密 ----------
# 不提交本地机密配置；如有 appsettings.*.local.json / .env.local 等
*.env.local
.env.local
.env.*.local
appsettings.*.local.json
# 本地开发配置（含真实数据库连接，仅本机使用，不入库）
backend/appsettings.Development.json
# 基础配置也包含 JWT 与密码加密密钥，仅本机使用
backend/appsettings.json

# ---------- 编辑器 / 系统 ----------
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# ---------- 上传文件目录（运行时产生） ----------
uploads/
